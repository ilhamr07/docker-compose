#membuat docker compose
docker compose create 

#menjalankan docker compose
docker compose start

#melihat docker compose
docker compose ps

#menghapus docker compose 
docker compose down

#melihat docker compose project name
docker compose ls

#docker events
docker events --filter 'container=mongodb-example'

#docker container stats
docker container stats

